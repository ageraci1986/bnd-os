export default function LoginPage() {
  // Phase 3.4 — UI implementation
  return (
    <main className="flex min-h-screen items-center justify-center p-8">
      <p>Login (à implémenter en Phase 3.4)</p>
    </main>
  );
}
